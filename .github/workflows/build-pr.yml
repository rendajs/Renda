name: build-pr

on:
  pull_request:
    branches: [main]

env:
  # be sure to also update these in ci.yml
  DENO_DIR: deno_dir
  DENO_VERSION: "1.25.3"

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      DENO_VERSION: ${{ env.DENO_VERSION }}
      DENO_DIR: ${{ env.DENO_DIR }}
