export class GPUQueue {
	/**
	 * @param {GPUBuffer} buffer
	 * @param {number} bufferOffset
	 * @param {ArrayBuffer | Uint8Array | DataView} data
	 * @param {number} dataOffset
	 * @param {number} size
	 */
	writeBuffer(buffer, bufferOffset, data, dataOffset, size) {}
}
